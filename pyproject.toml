[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Carson Professor"
authors = [{name = "Carson Rueter", email = "swurl@swurl.xyz"}]
dynamic = ["version", "description"]
